name: 'QIIME 2 Library Packaging'
description: 'Build, test, and distribute a QIIME 2 conda package'
inputs:
  conda-recipe-path:
    description: 'Path to Conda recipe'
    required: false
    default: 'ci/recipe'
  qiime2-recipe-path:
    description: 'Path to QIIME 2 recipe'
    required: false
    default: 'qiime2-recipe.yaml'
  package-name:
    description: 'Name of QIIME 2 package'
    required: true
  build-target:
    description: 'Target QIIME 2 release cycle: "tested" or "staged" or "released"'
    required: true
    default: 'released'
  additional-tests:
    description: 'Additional tests to run post-build'
    required: false
  library-token:
    description: 'A package token from library.qiime2.org. Required to upload built packages.'
    required: true
    default: ''
runs:
  using: 'node12'
  main: 'main/script.js'
